# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: skate-today
services:  
  app:
    language: dotnet
    project: ./skate-today.AppHost/skate-today.AppHost.csproj
    host: containerapp
